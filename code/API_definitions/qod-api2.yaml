openapi: 3.0.3
info:
  title: Sample API
  version: 1.0.0

paths:
  /switch/{switchId}:
    get:
      summary: Get s by ID
      parameters:
        - name: switchId
          in: path
          required: true
          schema:
            type: string
          description: ID
      responses:
        '200':
          description: Successful response
