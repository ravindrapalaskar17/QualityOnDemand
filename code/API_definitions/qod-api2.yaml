openapi: 3.0.3
info:
  title: Sample API
  version: 1.0.0

paths:
  /import/{Id}:
    get:
      summary: Get import by ID
      parameters:
        - name: Id
          in: path
          required: true
          schema:
            type: string
          description: import ID
      responses:
        '200':
          description: Successful response
