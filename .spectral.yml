rules:
  tag-description:
    description: Please provide a description for each tag.
    documentationUrl: https://www.example.com/docs/tag-description.md
    message: Info must have description "{{error}}"
    severity: hint
    given: $.info[*]
    then:
      field: description
      function: truthy
