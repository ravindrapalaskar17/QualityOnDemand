functions:
  - camara-reserved-words
functionsDir: "./lint_function"
rules:
  check-reserved-words:
    message: "{{error}}"
    severity: warn
    given: "$.paths.* | $.paths.*.* | $.paths.*.*.* | $.paths.*.*.*.*"
    then:
      function: camara-reserved-words
